  ابنِ الحزمة

```bash
dpkg-buildpackage -us -uc -j$(nproc)
```

راقب المخرجات، يفترض أن ترى أسطراً مثل:

```
-- Installing: /home/.../debian/startlens/usr/bin/startlens
-- Installing: /home/.../debian/startlens/usr/share/applications/startlens.desktop
...
```

7. تأكد من نجاح التغليف (قبل التثبيت)

الفحص الأول: حجم الحزمة

```bash
ls -lh ~/startlens_*.deb
```

يجب أن يكون الحجم الآن عدة ميغابايت (يعتمد على حجم الصور، غالباً بين 5-15 ميغابايت). إذا كان لا يزال 2.2 كيلوبايت، أرسل لي مخرجات البناء كاملة.

الفحص الثاني: محتويات الحزمة

```bash
dpkg -c ~/startlens_1.0.0-beta_amd64.deb
```

يجب أن ترى بالداخل على الأقل:

· usr/bin/startlens (البرنامج التنفيذي)
· usr/share/applications/startlens.desktop
· usr/share/icons/hicolor/scalable/apps/startlens.svg
· usr/share/startlens/images/bg1.jpg
· usr/share/startlens/images/bg2.png

إذا رأيت هذه الملفات فالتغليف ناجح 100%.

8. ثبّت الحزمة واختبر

```bash
sudo dpkg -i ~/startlens_*.deb
sudo apt install -f   # إذا طلب تبعيات
```

ثم شغّل:

```bash
startlens
· بعد تثبيت الحزمة، سيجد المستخدم أيقونتين على سطح المكتب:
  · StartLens (الاختصار العادي لتشغيل التطبيق).
  · إعداد StartLens (لتشغيل أداة الدمج مع اللوحة).
· عند النقر على أيقونة "إعداد StartLens"، سيتم دمج الزر تلقائياً في LXQt / XFCE، أو تفعيل tray مع تعليمات للبيئات الأخرى.
· بعد الإعداد، يمكن حذف أيقونة "إعداد StartLens" من سطح المكتب.

---
